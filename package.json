{
  "name": "tankwars",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Turn-based artillery game — a modernised Tank Wars, three modes: classic, modern, advanced",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node tools/prune-dist.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "assets": "py -3 tools/extract_sheets.py && py -3 tools/pack_atlas.py && py -3 tools/boss_mounts.py"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
